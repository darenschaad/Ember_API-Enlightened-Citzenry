import { concatCommitteeArrays } from 'enlightened-citizenry/helpers/concat-committee-arrays';
import { module, test } from 'qunit';

module('Unit | Helper | concat committee arrays');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concatCommitteeArrays([42]);
  assert.ok(result);
});
