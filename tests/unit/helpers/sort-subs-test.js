import { sortSubs } from 'enlightened-citizenry/helpers/sort-subs';
import { module, test } from 'qunit';

module('Unit | Helper | sort subs');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortSubs([42]);
  assert.ok(result);
});
